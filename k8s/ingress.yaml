apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ci-cd-demo
spec:
  ingressClassName: nginx
  rules:
    - host: demo.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ci-cd-demo
                port:
                  number: 8000
